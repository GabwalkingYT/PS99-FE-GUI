-- LocalScript inside StarterGui > ScreenGui
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local ScreenGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
ScreenGui.Name = "PS99_FE_GUI"
ScreenGui.ResetOnSpawn = false

-- MAIN FRAME
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Name = "MainFrame"
MainFrame.Position = UDim2.new(0.3, 0, 0.3, 0)
MainFrame.Size = UDim2.new(0, 300, 0, 500)
MainFrame.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
MainFrame.Active = true
MainFrame.Draggable = true

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Title.Text = "Ps99 OP GUI"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.Font = Enum.Font.GothamBlack
Title.TextSize = 20

local CloseBtn = Instance.new("TextButton", MainFrame)
CloseBtn.Position = UDim2.new(1, -30, 0, 5)
CloseBtn.Size = UDim2.new(0, 25, 0, 25)
CloseBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.new(1, 1, 1)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 14

local OpenBtn = Instance.new("TextButton", ScreenGui)
OpenBtn.Position = UDim2.new(0, 10, 0.5, -20)
OpenBtn.Size = UDim2.new(0, 100, 0, 40)
OpenBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
OpenBtn.Text = "Open GUI"
OpenBtn.TextColor3 = Color3.new(1, 1, 1)
OpenBtn.Font = Enum.Font.GothamBold
OpenBtn.TextSize = 14
OpenBtn.Visible = false

local function makeButton(name, y, text)
	local btn = Instance.new("TextButton", MainFrame)
	btn.Name = name
	btn.Position = UDim2.new(0, 20, 0, y)
	btn.Size = UDim2.new(0, 260, 0, 40)
	btn.Text = text
	btn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
	btn.TextColor3 = Color3.new(1, 1, 1)
	btn.Font = Enum.Font.Gotham
	btn.TextSize = 14
	return btn
end

local PetSpawnerBtn = makeButton("PetSpawnerBtn", 60, "Pet Spawner")
local GemSpawnerBtn = makeButton("GemSpawnerBtn", 110, "Gem Spawner")
local HoverboardBtn = makeButton("HoverboardBtn", 160, "Hoverboard Spawner")
local GamepassBtn = makeButton("GamepassBtn", 210, "Gamepass Spawner")
local TitanicSpawnerBtn = makeButton("TitanicSpawnerBtn", 260, "Titanic Spawner")
local AutoFarmBtn = makeButton("AutoFarmBtn", 310, "AutoFarm")
local InfYieldBtn = makeButton("InfYieldBtn", 360, "INF YIELD")

-- POPUP FRAME
local Popup = Instance.new("Frame", ScreenGui)
Popup.Position = UDim2.new(0.35, 0, 0.35, 0)
Popup.Size = UDim2.new(0, 250, 0, 150)
Popup.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
Popup.Visible = false

local PopupTitle = Instance.new("TextLabel", Popup)
PopupTitle.Size = UDim2.new(1, 0, 0, 40)
PopupTitle.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
PopupTitle.Text = "ULTIMATE SPAWNER"
PopupTitle.TextColor3 = Color3.new(1, 1, 1)
PopupTitle.Font = Enum.Font.GothamBold
PopupTitle.TextSize = 16

local InputBox = Instance.new("TextBox", Popup)
InputBox.Position = UDim2.new(0.1, 0, 0.4, 0)
InputBox.Size = UDim2.new(0.8, 0, 0, 30)
InputBox.PlaceholderText = "Spawn Anything Enjoy! 🌈"
InputBox.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
InputBox.TextColor3 = Color3.new(1, 1, 1)
InputBox.Font = Enum.Font.Gotham
InputBox.TextSize = 14

local SpawnBtn = Instance.new("TextButton", Popup)
SpawnBtn.Position = UDim2.new(0.25, 0, 0.7, 0)
SpawnBtn.Size = UDim2.new(0.5, 0, 0, 30)
SpawnBtn.Text = "Spawn"
SpawnBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
SpawnBtn.TextColor3 = Color3.new(1, 1, 1)
SpawnBtn.Font = Enum.Font.GothamBold
SpawnBtn.TextSize = 14

-- Spawned Confirmation Popup
local SpawnedPopup = Instance.new("Frame", ScreenGui)
SpawnedPopup.Position = UDim2.new(0.35, 0, 0.35, 0)
SpawnedPopup.Size = UDim2.new(0, 200, 0, 100)
SpawnedPopup.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
SpawnedPopup.Visible = false

local SpawnedLabel = Instance.new("TextLabel", SpawnedPopup)
SpawnedLabel.Size = UDim2.new(1, 0, 1, 0)
SpawnedLabel.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
SpawnedLabel.Text = "Spawned!!"
SpawnedLabel.TextColor3 = Color3.new(1, 1, 1)
SpawnedLabel.Font = Enum.Font.GothamBold
SpawnedLabel.TextSize = 20
SpawnedLabel.TextAlign = Enum.TextXAlignment.Center
SpawnedLabel.TextYAlignment = Enum.TextYAlignment.Center

-- Show "Spawned!!" Popup
local function showSpawnedPopup()
	SpawnedPopup.Visible = true
	wait(2)  -- Show for 2 seconds
	SpawnedPopup.Visible = false
end

local currentSpawner = nil

-- Show popup function
local function showPopup(spawnerType)
	currentSpawner = spawnerType
	Popup.Visible = true
	InputBox.Text = ""
end

-- GUI logic
CloseBtn.MouseButton1Click:Connect(function()
	MainFrame.Visible = false
	OpenBtn.Visible = true
end)

OpenBtn.MouseButton1Click:Connect(function()
	MainFrame.Visible = true
	OpenBtn.Visible = false
end)

PetSpawnerBtn.MouseButton1Click:Connect(function() showPopup("pet") end)
GemSpawnerBtn.MouseButton1Click:Connect(function() showPopup("gem") end)
HoverboardBtn.MouseButton1Click:Connect(function() showPopup("hoverboard") end)
GamepassBtn.MouseButton1Click:Connect(function() showPopup("gamepass") end)
TitanicSpawnerBtn.MouseButton1Click:Connect(function() showPopup("titanic") end)
AutoFarmBtn.MouseButton1Click:Connect(function()
	print("AutoFarm activated")
	-- Insert your AutoFarm logic here
end)

SpawnBtn.MouseButton1Click:Connect(function()
	local amount = tonumber(InputBox.Text)
	if amount and currentSpawner then
		Popup.Visible = false
		print("Spawning", amount, currentSpawner)
		-- Insert your spawner logic here

		-- Show the Spawned confirmation popup
		showSpawnedPopup()
	end
end)

InfYieldBtn.MouseButton1Click:Connect(function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()
end)

-- 🌈 Rainbow Background Animation
local hue = 0
RunService.RenderStepped:Connect(function()
	hue = (hue + 0.005) % 1
	local color = Color3.fromHSV(hue, 1, 1)
	MainFrame.BackgroundColor3 = color
	Popup.BackgroundColor3 = color
end)
